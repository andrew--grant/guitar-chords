function String(stringNumber, finger) {
    this.string = string;
    this.fret = fret;
    this.circle = new createjs.Shape();
    this.fingerColour = "DeepSkyBlue";
}

String.prototype.draw = function (x, y) {
     
}
